# Liam's Game Item Icons

Production custom item artwork goes here.

Use bespoke storybook object art, not stock icon packs. Keep each item readable at small UI sizes and preserve the emoji fallback in `src/data/items.ts`.

Register finished icons in:

```text
src/data/itemArtwork.ts
```

See:

```text
docs/art/item-icons/liams-game-custom-item-icon-plan.md
```
