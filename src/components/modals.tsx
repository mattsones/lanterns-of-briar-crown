// @ts-nocheck
import { HERO_GROWTH_OPTIONS } from "../data/character";
import { COMPANION_OPTIONS } from "../data/companions";
import { BATTLE_CONSUMABLES, ITEM_DB } from "../data/items";
import { getDialoguePortrait } from "../data/portraits";
import { RECIPE_DB } from "../data/recipes";
import { getHeroAvatar } from "../game/appearance";
import { getCompanionCommandHint } from "../game/companions";
import { checkSummary, resolveSkillCheck } from "../game/dice";
import { canCraftRecipe, formatIngredients, gainItem, getBuyPrice, getItemHighlights, getSellPrice } from "../game/inventory";
import { formatSaveTimestamp } from "../game/save";
import { formatBonuses, getDerivedStats } from "../game/stats";
import { Button, ChoiceButton, Meter } from "./ui";

export function LevelUpModal({ player, target, choose }) {
  return <div className="fixed inset-0 z-[90] flex items-end justify-center bg-black/70 p-4 sm:items-center">
    <div className="w-full max-w-4xl rounded-[2rem] border border-sky-300/20 bg-slate-900 p-5 shadow-2xl">
      <div className="text-center">
        <div className="text-5xl">✨</div>
        <div className="mt-2 text-3xl font-bold">Level Up!</div>
        <div className="mt-1 text-white/70">{player.name} reached Level {(player.level || 1) + 1}. XP {player.xp}/{target}</div>
        <div className="mt-3 rounded-2xl border border-sky-300/20 bg-sky-400/10 p-3 text-sm text-white/80">Every level gives <span className="font-semibold text-sky-200">Max HP +4</span> and <span className="font-semibold text-sky-200">Current HP +4</span>. Choose how your hero grows.</div>
      </div>
      <div className="mt-5 grid gap-3 md:grid-cols-2 xl:grid-cols-5">
        {HERO_GROWTH_OPTIONS.map((growth) => <button key={growth.id} onClick={() => choose(growth)} className="rounded-3xl border border-white/10 bg-white/5 p-4 text-left transition hover:border-sky-300/40 hover:bg-sky-400/10">
          <div className="text-3xl">{growth.icon}</div>
          <div className="mt-2 text-lg font-semibold">{growth.name}</div>
          <div className="mt-1 text-sm text-emerald-300">{formatBonuses(growth.bonuses)}</div>
          <div className="mt-2 text-xs leading-5 text-white/70">{growth.description}</div>
        </button>)}
      </div>
    </div>
  </div>;
}

export function DialogueModal({ dialogue }) {
  const portraitAsset = getDialoguePortrait(dialogue.name);

  return <div className="fixed inset-0 z-[80] flex items-end justify-center bg-black/60 p-4 sm:items-center">
    <div className="flex max-h-[85vh] w-full max-w-3xl flex-col overflow-hidden rounded-[2rem] border border-white/10 bg-slate-900 p-5 shadow-2xl">
      <div className="min-h-0 flex-1 overflow-y-auto pr-1">
        <div className="flex items-start gap-4">
          <div className="relative flex h-24 w-24 shrink-0 items-center justify-center overflow-hidden rounded-3xl bg-white/10 text-4xl">
            <span>{dialogue.portrait}</span>
            {portraitAsset ? <img src={portraitAsset.src} alt={portraitAsset.alt} onError={(event) => { event.currentTarget.style.display = "none"; }} className="absolute inset-0 h-full w-full object-cover" /> : null}
          </div>
          <div className="min-w-0 flex-1">
            <div className="text-xl font-semibold">{dialogue.name}</div>
            {dialogue.messages ? <div className="mt-3 space-y-2">{dialogue.messages.map((m, i) => <div key={`${m.speaker}-${i}`} className={`flex ${m.side === "right" ? "justify-end" : "justify-start"}`}><div className={`max-w-[85%] rounded-2xl px-4 py-3 text-base leading-7 ${m.side === "right" ? "bg-emerald-500/20" : "bg-white/10 text-white/85"}`}><div className="mb-1 text-[10px] uppercase tracking-wide text-white/50">{m.speaker}</div><div>{m.text}</div></div></div>)}</div> : <div className="mt-2 whitespace-pre-line text-base leading-7 text-white/85">{dialogue.text}</div>}
          </div>
        </div>
      </div>
      <div className="mt-5 grid gap-2 border-t border-white/10 pt-4">{dialogue.choices.map((choice, i) => <ChoiceButton key={`${choice.label}-${i}`} choice={choice} onChoose={(selected) => selected.effect?.()} />)}</div>
    </div>
  </div>;
}
export function InteriorModal({ scene, close, flags, setFlags, player, setPlayer, companion, setCompanion, setActiveCompanion, dismissCompanion, saveGame, announce, setCraftOpen, setDialogue }) {
  const getRecruitmentScene = (option) => {
    const scenes = {
      rowan: { opening: "Rowan Reedshield sits near the inn's side wall, not at a table but beside it, where he can see both the front door and the stairs. A scratched shield rests across his knees. He is polishing out a dent slowly, not because the shield needs polish, but because the work gives his hands somewhere calm to be. When a cart-driver bumps into a serving girl, Rowan rises halfway before anyone else notices. The girl steadies the tray, the driver apologizes, and Rowan sits again without asking to be thanked.", ask: "He looks up when you approach. “If you're looking for someone to swing first and think later, keep walking. If you're looking for someone to make sure people come home, sit down.”", goodLabel: "People are in danger. I need someone who protects first and boasts never.", goodReply: "Rowan studies you for a long moment, then sets the shield strap properly across his shoulder. “Good answer. Trouble is loud enough without us adding noise. I'll come. If the road is being trained to fear the wrong thing, then we keep our heads, keep our line, and bring people home.”", badLabel: "I need someone sturdy enough to stand in front of me.", badReply: "Rowan's expression closes like a gate. “A shield is not furniture, and neither am I. Come back when you are asking for a companion, not a wall with boots.”", neutralLabel: "What kind of trouble are you expecting?", neutralReply: "“The organized kind,” Rowan says. “The kind that counts on frightened people shoving each other aside. That's when someone steady matters most. Ask me straight if you want me with you.”" },
      tilda: { opening: "Tilda Quickstep is perched backward on a chair near the hearth, flicking apple seeds into a tin cup across the room. She misses twice on purpose, hits once without looking, and grins when a nearby card player checks his sleeve for missing coins. There is a little chaos around her, but it is tidy chaos—the sort that trips a bully, not a child.", ask: "She tilts her head at you. “Let me guess. Road trouble, fake orders, dramatic cellar noises, and someone important said ‘be careful’ in a voice that made careful sound impossible?”", goodLabel: "I need someone clever enough to spot traps and quick enough to laugh at them.", goodReply: "Tilda snaps her fingers and points at you like you just solved a riddle. “Finally, someone with respect for proper nonsense. Yes. I'm in. If we're chasing people who plant fake orders, I want to see their faces when the wrong person finds the right clue.”", badLabel: "Just follow my orders and don't ask questions.", badReply: "Tilda makes a wounded sound and presses a hand to her heart. “No questions? In this economy of suspicious paper? Absolutely not. Try again when you need a partner instead of a shadow.”", neutralLabel: "You seem like you already know what's happening.", neutralReply: "“I know what people look like when they're lying badly,” Tilda says. “And half this town looks like it has been handed a lie and told to hold it carefully. That's interesting. Dangerous, but interesting.”" },
      moss: { opening: "Moss Fenmere sits where the firelight thins, turning a cracked teacup between both hands. No steam rises from it, but the surface ripples whenever the inn beams creak. A child nearby whispers that the cellar is haunted. Moss does not correct the child at first. They listen, then say gently, “Haunted is a word people use when history has not finished speaking.”", ask: "Moss looks at you as if they have already heard the question arrive. “The road is frightened above ground, and the roots are restless below it. That is not two problems.”", goodLabel: "There is old magic under this. I need someone patient enough to notice what others miss.", goodReply: "Moss smiles faintly. “Patience is how the quiet things become readable. I will come. But understand this: old places do not only hide monsters. They hide reasons. We should be prepared to find both.”", badLabel: "Can you do spooky magic at things until the problem goes away?", badReply: "Moss blinks once. “Magic is not a broom for sweeping fear under a rug. Come back when you are ready to listen as much as act.”", neutralLabel: "What do you mean the road and roots are one problem?", neutralReply: "“Roads are promises,” Moss says. “Roots are memory. Someone is using one to disturb the other. That is why everyone feels the wrongness before they understand it.”" },
    };
    return scenes[option.id];
  };

  const recruitConversation = (option) => {
    const scene = getRecruitmentScene(option);
    if (!scene) return;
    const stats = getDerivedStats(player);
    const checkCfg = option.id === "rowan" ? { stat: "Heart", dc: 10 } : option.id === "tilda" ? { stat: "Wit", dc: 10 } : { stat: "Will", dc: 10 };
    const join = (check = null) => {
      const bonusXp = check?.success ? 5 : 0;
      setActiveCompanion(option.id);
      if (bonusXp) setCompanion((c) => ({ ...c, xp: (c.xp || 0) + bonusXp }));
      close();
      setDialogue({ portrait: option.icon, name: option.name, text: `${check ? `${checkSummary(check)}

` : ""}${scene.goodReply}${bonusXp ? `

${option.name} starts with +${bonusXp} companion XP because your approach earned immediate trust.` : ""}`, choices: [{ label: "Welcome to the party.", effect: () => setDialogue(null) }] });
    };
    const attemptRecruit = () => {
      const check = resolveSkillCheck(stats, checkCfg.stat, checkCfg.dc);
      if (check.success) return join(check);
      setDialogue({ portrait: option.icon, name: option.name, text: `${checkSummary(check)}

Your approach is right, but the words land awkwardly in the noise of the inn. ${option.name} does not walk away. They wait to see whether you mean it plainly.`, choices: [{ label: "Say it plainly and ask them to come.", effect: () => join(check) }, { label: "Step back for now.", effect: () => setDialogue(null) }] });
    };
    setDialogue({ portrait: option.icon, name: option.name, text: `${scene.opening}

${scene.ask}`, choices: [
      { label: scene.goodLabel, requirement: `${checkCfg.stat} Check DC ${checkCfg.dc}`, effect: attemptRecruit },
      { label: scene.neutralLabel, effect: () => setDialogue({ portrait: option.icon, name: option.name, text: scene.neutralReply, choices: [{ label: scene.goodLabel, requirement: `${checkCfg.stat} Check DC ${checkCfg.dc}`, effect: attemptRecruit }, { label: "I'll think about it.", effect: () => setDialogue(null) }] }) },
      { label: scene.badLabel, effect: () => { setFlags((prev) => ({ ...prev, [`${option.id}Status`]: "declined" })); setDialogue({ portrait: option.icon, name: option.name, text: scene.badReply, choices: [{ label: "Fair. I'll rethink that.", effect: () => setDialogue(null) }] }); } },
    ] });
  };

  return <div className="fixed inset-0 z-40 flex items-end justify-center bg-black/50 p-4 sm:items-center"><div className="max-h-[88vh] w-full max-w-5xl overflow-y-auto rounded-[2rem] border border-white/10 bg-slate-900 p-5 shadow-2xl"><div className="mb-4 flex items-center justify-between gap-3"><div><div className="text-2xl font-semibold">{scene === "home" ? "Inside Your Home" : scene === "bramInn" ? "Inside the Bramblecross Inn" : scene === "watchhouse" ? "Inside the Watchhouse" : "Interior"}</div><div className="text-sm text-white/70">{scene === "watchhouse" && !flags.ennaBriefed ? "An incomplete case wall waits for your road report." : "A focused interior scene."}</div></div><Button onClick={close}>Leave</Button></div>
    {scene === "home" ? <div className="space-y-4"><div className="rounded-3xl border border-white/10 bg-white/5 p-5 text-sm text-white/80">A warm, familiar room. Your own things suddenly feel more important now that the road has gone dangerous.</div><div className="flex flex-wrap gap-2"><Button onClick={() => { setPlayer((p) => ({ ...p, hp: Math.min(p.maxHp, p.hp + 6) })); }}>Rest a little</Button>{!flags.homeStashClaimed ? <Button onClick={() => { gainItem(setPlayer, "old_hatchet", 1); setFlags((f) => ({ ...f, homeStashClaimed: true })); announce("You gather your old village hatchet from home.", [{ id: "old_hatchet", qty: 1 }]); }}>Take your old hatchet</Button> : null}</div></div> : null}
    {scene === "bramInn" ? <div className="space-y-4"><div className="rounded-3xl border border-white/10 bg-white/5 p-5 text-sm text-white/80">The Bramblecross Inn is trying very hard to feel ordinary. Mugs clink, someone laughs too loudly, and every traveler in the room seems to be listening for news from the road. Three capable strangers stand out—not because they are waiting to be hired, but because each of them is already responding to the crisis in their own way. Recruitment is conversation-driven: the way you speak to them matters.</div><div className="flex flex-wrap gap-2">{companion.recruited ? <Button onClick={dismissCompanion}>Ask current companion to wait here</Button> : null}<Button onClick={() => { setPlayer((p) => ({ ...p, hp: p.maxHp })); setCompanion((c) => c.recruited ? { ...c, hp: c.maxHp } : c); saveGame("Bramblecross Inn"); }}>Rest for the night</Button></div><div className="grid gap-3 md:grid-cols-3">{Object.values(COMPANION_OPTIONS).map((o) => <div key={o.id} className="rounded-3xl border border-white/10 bg-white/5 p-4"><div className="relative flex aspect-[4/3] items-center justify-center overflow-hidden rounded-2xl bg-white/10 text-4xl"><span>{o.icon}</span>{o.portraitSrc ? <img src={o.portraitSrc} alt={`Portrait of ${o.name}`} onError={(event) => { event.currentTarget.style.display = "none"; }} className="absolute inset-0 h-full w-full object-cover" /> : null}</div><div className="mt-2 text-lg font-semibold">{o.name}</div><div className="text-sm text-emerald-300">{o.role}</div><div className="mt-2 text-sm text-white/75">{o.description}</div><div className="mt-2 text-xs text-white/55">{o.id === "rowan" ? "Polishing a dented shield while watching the door." : o.id === "tilda" ? "Making apple seeds land where apple seeds should not." : "Listening to the fire as if it is telling the truth slowly."}</div><div className="mt-4"><Button onClick={() => recruitConversation(o)}>{companion.id === o.id ? "Traveling" : "Talk"}</Button></div></div>)}</div></div> : null}
    {scene === "watchhouse" ? <div className="space-y-4">
      {!flags.ennaBriefed ? <>
        <div className="grid gap-4 lg:grid-cols-[0.9fr_1.1fr]">
          <div className="rounded-3xl border border-white/10 bg-white/5 p-5">
            <div className="text-sm uppercase tracking-wide text-white/50">Case Wall</div>
            <div className="mt-2 text-xl font-semibold">Incomplete Pattern</div>
            <div className="mt-3 rounded-2xl border border-amber-300/20 bg-amber-400/10 p-4 text-sm text-white/80">The watchhouse wall is full of Bramblecross-only fragments. Enna has pins, string, and worried handwriting—but your Hearthhollow and Lantern Road discoveries are still missing.</div>
          </div>
          <div className="grid gap-3 sm:grid-cols-2">
            <div className="rounded-3xl border border-white/10 bg-black/20 p-4"><div className="text-2xl">📦</div><div className="mt-2 font-semibold">Missing Porters</div><div className="mt-1 text-sm text-white/65">Last assigned near the old root cellar.</div></div>
            <div className="rounded-3xl border border-white/10 bg-black/20 p-4"><div className="text-2xl">📄</div><div className="mt-2 font-semibold">Copied Orders</div><div className="mt-1 text-sm text-white/65">The seal looks official until someone reads closely.</div></div>
            <div className="rounded-3xl border border-white/10 bg-black/20 p-4"><div className="text-2xl">🧺</div><div className="mt-2 font-semibold">Supply Delays</div><div className="mt-1 text-sm text-white/65">Carts and crates arriving late, wrong, or not at all.</div></div>
            <div className="rounded-3xl border border-dashed border-white/20 bg-black/10 p-4"><div className="text-2xl">❓</div><div className="mt-2 font-semibold">Road-Side Evidence Missing</div><div className="mt-1 text-sm text-white/65">Report to Enna to complete the picture.</div></div>
          </div>
        </div>
        <div className="rounded-3xl border border-amber-300/20 bg-amber-400/10 p-4 text-sm text-white/85">Enna notices the road dust on your boots. Talk to her at the clerk's desk before trusting the wall as complete.</div>
      </> : <>
        <div className="rounded-3xl border border-emerald-300/20 bg-emerald-400/10 p-5">
          <div className="text-sm uppercase tracking-wide text-emerald-200/80">Completed Case Wall</div>
          <div className="mt-2 text-2xl font-semibold">False authority growing over true roads</div>
          <div className="mt-2 text-sm leading-6 text-white/80">Your field report has changed the shape of the investigation. Bramblecross paperwork, Hearthhollow panic, Lantern Road evidence, missing cargo, and cellar rumors now point to one route-based pattern.</div>
        </div>
        <div className="grid gap-3 lg:grid-cols-5">
          <div className="rounded-3xl border border-white/10 bg-black/20 p-4"><div className="text-2xl">🐗</div><div className="mt-2 font-semibold">Lio's Satchel</div><div className="mt-1 text-xs text-white/60">Cut loose before the village panic.</div></div>
          <div className="rounded-3xl border border-white/10 bg-black/20 p-4"><div className="text-2xl">🗿</div><div className="mt-2 font-semibold">Planted Order</div><div className="mt-1 text-xs text-white/60">Meant to be discovered at the milestone.</div></div>
          <div className="rounded-3xl border border-white/10 bg-black/20 p-4"><div className="text-2xl">🧺</div><div className="mt-2 font-semibold">Willow Seal</div><div className="mt-1 text-xs text-white/60">Trusted cargo mark may be stolen.</div></div>
          <div className="rounded-3xl border border-white/10 bg-black/20 p-4"><div className="text-2xl">🕳️</div><div className="mt-2 font-semibold">Root Cellar</div><div className="mt-1 text-xs text-white/60">Missing porters and old routes below town.</div></div>
          <div className="rounded-3xl border border-fuchsia-300/20 bg-fuchsia-400/10 p-4"><div className="text-2xl">👑</div><div className="mt-2 font-semibold">False Crown?</div><div className="mt-1 text-xs text-white/60">Not a royal seal. Not random.</div></div>
        </div>
        <div className="grid gap-3 sm:grid-cols-2">
          <Button className="justify-start text-left" onClick={() => { setFlags((f) => ({ ...f, watchEvidenceRead: true })); const check = resolveSkillCheck(getDerivedStats(player), "Wit", 10); setDialogue({ portrait: "🧷", name: "Evidence Board", text: `${checkSummary(check)}

${check.success ? "The red string no longer runs from clue to clue. It runs from system to system: road signs, cargo marks, public notices, old cellars, and frightened people. The pattern is not theft or random monster trouble. It is misdirection built to make ordinary systems lie." : "The board is dense and crowded, but the big shape still emerges: road signs, cargo marks, public notices, and old cellars are all being used together. The pattern is misdirection."}`, choices: [{ label: "That pattern matters.", effect: () => setDialogue(null) }] }); }}>🧷 Study the completed evidence board</Button>
          <Button className="justify-start text-left" onClick={() => { setFlags((f) => ({ ...f, watchLedgerRead: true })); const check = resolveSkillCheck(getDerivedStats(player), "Wit", 10); setDialogue({ portrait: "📚", name: "Duty Ledger", text: `${checkSummary(check)}

${check.success ? "The missing porters were last assigned near the old root cellar. Someone altered the route notes afterward, turning an ordinary storage shift into a blind corner in the records. The changed handwriting is trying to imitate the clerk's shorthand, but the route marks are copied backward." : "The missing porters were last assigned near the old root cellar. Someone altered the route notes afterward, turning an ordinary storage shift into a blind corner in the records."}`, choices: [{ label: "Useful.", effect: () => setDialogue(null) }] }); }}>📚 Read the duty ledger</Button>
          <Button className="justify-start text-left" onClick={() => { setFlags((f) => ({ ...f, watchMapRead: true })); const check = resolveSkillCheck(getDerivedStats(player), "Instinct", 10); setDialogue({ portrait: "🗺️", name: "Wall Map", text: `${checkSummary(check)}

${check.success ? "Pins connect Hearthhollow, Lantern Road, Bramblecross, and cellar paths under the warehouses. Some lines are public roads. Some are older, half-erased routes that still match the marks at the Lantern Shrine. One faded line bends west into a cluster of obsolete marks Enna has circled twice." : "Pins connect Hearthhollow, Lantern Road, Bramblecross, and cellar paths under the warehouses. Some lines are public roads. Some are older, half-erased routes that still match the marks at the Lantern Shrine."}`, choices: [{ label: "Memorize it.", effect: () => setDialogue(null) }] }); }}>🗺️ Examine the wall map</Button>
          <Button className="justify-start text-left" onClick={() => { setFlags((f) => ({ ...f, watchOrdersRead: true })); const check = resolveSkillCheck(getDerivedStats(player), "Will", 10); setDialogue({ portrait: "📄", name: "Forged Orders File", text: `${checkSummary(check)}

${check.success ? "The copied orders borrow royal language but push fear instead of wise command. Enna has underlined the verbs: delay, hold, misdirect, recover. Whoever wrote them thinks in routes and pressure points. The tone feels like a crown without care: command stripped of responsibility." : "The copied orders borrow royal language but push fear instead of wise command. Enna has underlined the verbs: delay, hold, misdirect, recover. Whoever wrote them thinks in routes and pressure points."}`, choices: [{ label: "The tone itself is fake.", effect: () => setDialogue(null) }] }); }}>📄 Read the forged orders file</Button>
        </div>
        <div className="rounded-3xl border border-white/10 bg-black/20 p-4 text-sm text-white/75">Enna has left a note in the corner: <span className="italic text-white">“A road is safest when truth walks it first. Someone is making lies walk first.”</span></div>
      </>}
    </div> : null}</div></div>;
}
export function ShopModal({ shop, player, close, buyItem, sellItem, shopMode, flags }) { return <div className="fixed inset-0 z-30 overflow-y-auto bg-black/50 p-4"><div className="mx-auto my-8 max-w-3xl rounded-[2rem] border border-white/10 bg-slate-900 p-5"><div className="mb-4 flex justify-between"><div><div className="text-2xl font-semibold">{shop.title}</div><div className="text-sm text-yellow-300">Gold: {player.gold}</div>{shopMode === "smith" && flags.elderGavePurse && !flags.smithStarterDiscountUsed ? <div className="text-sm text-emerald-300">Starter discount available.</div> : null}</div><Button onClick={close}>Close</Button></div><div className="grid gap-5 lg:grid-cols-2"><div><div className="mb-2 text-sm font-semibold text-emerald-300">Buy</div><div className="grid gap-3">{shop.inventory.map((id) => { const item = ITEM_DB[id]; const discount = shopMode === "smith" && item?.slot && flags.elderGavePurse && !flags.smithStarterDiscountUsed ? 4 : shopMode === "market" && flags.marketDiscount ? 2 : 0; const cost = Math.max(1, getBuyPrice(id) - discount); return <div key={id} className="rounded-2xl border border-white/10 bg-white/5 p-3"><div className="flex justify-between gap-3"><div><div className="font-medium">{item.icon} {item.name}</div><div className="mt-1 text-xs text-white/70">{item.description}</div>{getItemHighlights(item).slice(0, 2).map((line) => <div key={line} className="mt-1 text-[11px] text-emerald-300/90">{line}</div>)}</div><Button onClick={() => buyItem(id)} disabled={player.gold < cost}>Buy • {cost}g</Button></div></div>; })}</div></div><div><div className="mb-2 text-sm font-semibold text-amber-300">Sell</div><div className="grid gap-3">{Object.entries(player.inventory).filter(([, c]) => c > 0).map(([id, count]) => <div key={id} className="rounded-2xl border border-white/10 bg-white/5 p-3"><div className="flex justify-between gap-3"><div>{ITEM_DB[id]?.icon} {ITEM_DB[id]?.name} <span className="text-xs text-white/60">x{count}</span></div><Button onClick={() => sellItem(id)}>Sell • {getSellPrice(id)}g</Button></div></div>)}</div></div></div></div></div>; }
export function CraftModal({ player, close, craftRecipe }) { return <div className="fixed inset-0 z-30 overflow-y-auto bg-black/50 p-4"><div className="mx-auto my-8 max-w-2xl rounded-[2rem] border border-white/10 bg-slate-900 p-5"><div className="mb-4 flex justify-between"><div><div className="text-2xl font-semibold">Potion Shed</div><div className="text-sm text-white/70">Brew something useful or memorable.</div></div><Button onClick={close}>Close</Button></div><div className="space-y-3">{Object.values(RECIPE_DB).map((recipe) => { const item = ITEM_DB[recipe.resultId]; return <div key={recipe.id} className="rounded-2xl border border-white/10 bg-white/5 p-4"><div className="flex justify-between gap-3"><div><div className="font-medium">{item.icon} {recipe.name}</div><div className="mt-1 text-sm text-white/75">{recipe.note}</div><div className="mt-2 text-xs text-white/60">Ingredients: {formatIngredients(recipe.ingredients)}</div></div><Button onClick={() => craftRecipe(recipe.id)} disabled={!canCraftRecipe(player, recipe)}>Craft</Button></div></div>; })}</div></div></div>; }
export function BattleModal({ battle, player, companion, heroSkills, heroAttack, finishBattle, battleItemsOpen, setBattleItemsOpen, useBattleConsumable }) { return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/90 p-4"><div className="w-full max-w-6xl rounded-[2rem] border border-white/10 bg-slate-900 p-5"><div className="grid gap-5 lg:grid-cols-[1.2fr_0.8fr]"><div><div className="mb-4 flex justify-between"><div><div className="text-2xl font-bold">Battle • {battle.enemy.name}</div><div className="text-sm text-white/70">Enemy intent: {battle.enemy.intent}</div><div className="text-xs text-white/50">Enemy {Math.max(1, (battle.totalEnemies || 1) - (battle.queue?.length || 0))} of {battle.totalEnemies || 1}</div></div><Button onClick={finishBattle} disabled={!battle.finished}>{battle.finished ? (battle.turn === "victory" || battle.enemy.hp <= 0 ? "Claim Victory" : "You Died • Return") : "Battle in Progress"}</Button></div><div className="grid gap-4 md:grid-cols-2"><div className="rounded-3xl border border-white/10 bg-black/20 p-4"><div className="text-5xl">{getHeroAvatar(player)}</div><div className="mt-2 text-xl font-semibold">{player.name}</div><div className="mt-3 space-y-3"><Meter value={player.hp} max={player.maxHp} label="Hero HP" />{companion.recruited ? <Meter value={companion.hp} max={companion.maxHp} label={`${companion.name} HP`} colorClass="bg-rose-400" /> : null}</div></div><div className="rounded-3xl border border-white/10 bg-black/20 p-4 text-right"><div className="text-6xl">{battle.enemy.icon}</div><div className="mt-2 text-xl font-semibold">{battle.enemy.name}</div><Meter value={battle.enemy.hp} max={battle.enemy.maxHp} label="Enemy HP" colorClass="bg-orange-400" />{battle.queue?.length ? <div className="mt-3 text-left text-sm text-white/70">Next: {battle.queue.map((e) => e.name).join(", ")}</div> : null}</div></div><div className="mt-4 rounded-3xl border border-white/10 bg-black/20 p-4"><div className="mb-3 text-sm font-medium text-white/80">Battle Log</div><div className="space-y-2 text-sm text-white/75">{battle.log.map((entry, i) => <div key={`${entry}-${i}`} className="rounded-2xl bg-white/5 px-3 py-2">{entry}</div>)}</div></div></div><div className="rounded-3xl border border-white/10 bg-black/20 p-4"><div className="mb-3 text-lg font-semibold">Actions</div><div className="mb-3 text-sm text-white/70">{battle.turn === "hero" ? "Choose Liam's action." : battle.turn === "companion" ? `${companion.name} is acting.` : battle.turn === "enemy" ? `${battle.enemy.name} is acting…` : battle.turn === "victory" ? "Victory!" : "Defeat…"}</div><div className="grid gap-2">{heroSkills.map((skill) => { const cooldownRemaining = battle.cooldowns?.[skill.id] || 0; return <button key={skill.name} onClick={() => heroAttack(skill)} disabled={battle.turn !== "hero" || battle.finished || cooldownRemaining > 0} className="rounded-2xl border border-white/10 bg-white/10 px-3 py-3 text-left hover:bg-white/20 disabled:opacity-40"><div className="flex items-center justify-between gap-2"><div className="font-medium">{skill.name}</div>{cooldownRemaining > 0 ? <div className="rounded-full bg-sky-400/20 px-2 py-0.5 text-[10px] text-sky-200">Ready in {cooldownRemaining}</div> : null}</div><div className="mt-1 text-xs text-white/70">{skill.description}</div></button>; })}<Button onClick={() => setBattleItemsOpen((p) => !p)} disabled={battle.turn !== "hero" || battle.finished}>{battleItemsOpen ? "Hide Items" : "Items"}</Button></div>{battleItemsOpen ? <div className="mt-4 rounded-2xl border border-white/10 bg-white/5 p-3"><div className="mb-2 text-xs uppercase tracking-wide text-white/50">Battle Pouch</div>{["slot1", "slot2"].map((slot, i) => { const id = player.battlePouch?.[slot]; if (!id || !BATTLE_CONSUMABLES[id]) return <div key={slot} className="mb-2 rounded-2xl border border-white/10 bg-black/20 p-3 text-sm text-white/65">Battle Pouch Slot {i + 1} is empty.</div>; const cfg = BATTLE_CONSUMABLES[id]; const qty = player.inventory?.[id] || 0; return <div key={slot} className="mb-2 rounded-2xl border border-white/10 bg-black/20 p-3"><div className="flex justify-between gap-3"><div><div className="font-medium">{ITEM_DB[id].icon} {cfg.name} <span className="text-xs text-white/60">x{qty}</span></div><div className="mt-1 text-xs text-white/70">Restores {cfg.heal} HP.</div></div><div className="flex flex-wrap gap-2"><Button onClick={() => useBattleConsumable(id, "hero")} disabled={qty <= 0 || player.hp >= player.maxHp}>Use on Liam</Button>{companion.recruited ? <Button onClick={() => useBattleConsumable(id, "companion")} disabled={qty <= 0 || companion.hp >= companion.maxHp}>Give to {companion.name}</Button> : null}</div></div></div>; })}</div> : null}{companion.recruited ? <div className="mt-4 rounded-2xl border border-white/10 bg-white/5 p-3 text-xs text-white/70">Companion command: <span className="text-white">{companion.command}</span><div className="mt-1 text-white/60">{getCompanionCommandHint(companion)}</div></div> : null}</div></div></div></div>; }
export function SaveModal({ mode, slots, drafts, setDrafts, close, save, load, loadCheckpoint }) { const isSave = mode === "save"; return <div className="fixed inset-0 z-[70] overflow-y-auto bg-black/60 p-4"><div className="mx-auto my-8 max-w-4xl rounded-[2rem] border border-white/10 bg-slate-900 p-5"><div className="mb-4 flex justify-between"><div><div className="text-2xl font-semibold">{isSave ? "Save Slots" : "Load Save Slot"}</div><div className="text-sm text-white/70">Multiple named saves for testing branches.</div></div><Button onClick={close}>Close</Button></div>{!isSave ? <div className="mb-3 rounded-3xl border border-sky-300/20 bg-sky-400/10 p-4"><Button onClick={loadCheckpoint}>Load Latest Checkpoint</Button></div> : null}<div className="grid gap-3">{slots.map((slot) => <div key={slot.id} className="rounded-3xl border border-white/10 bg-white/5 p-4"><div className="flex flex-wrap justify-between gap-3"><div><div className="text-sm uppercase tracking-wide text-white/50">Slot {slot.id}</div><div className="mt-1 text-lg font-semibold">{slot.name || `Empty Slot ${slot.id}`}</div><div className="mt-1 text-xs text-white/50">{formatSaveTimestamp(slot.updatedAt)}</div></div>{isSave ? <Button onClick={() => save(slot.id)}>{slot.payload ? "Overwrite" : "Save Here"}</Button> : <Button onClick={() => load(slot.id)} disabled={!slot.payload}>Load</Button>}</div>{isSave ? <input className="mt-3 w-full rounded-2xl border border-white/10 bg-black/20 px-3 py-2 text-white outline-none" value={drafts[slot.id] || ""} placeholder="Save name" onChange={(e) => setDrafts((p) => ({ ...p, [slot.id]: e.target.value }))} /> : null}</div>)}</div></div></div>; }
